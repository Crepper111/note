# 缺省源2.0
```cpp
#include<bits/stdc++.h>
#define SET(a,b) memset(a,b,sizeof(a))
#define PTMID ' '
#define PTEND '\n'
using namespace std;int read(){int rF=1,rK=0;char rC = getchar();while(rC<'0'||rC>'9'){if(rC=='-')rF=-1;rC=getchar();}while(rC>='0'&&rC<='9')rK=rK*10+rC-'0',rC=getchar();return rF*rK;}template<typename T,typename...T2>void read(T&&RD,T2&&...RD2){if constexpr(is_same_v<T,int&>)RD=read();else cin>>RD;if constexpr(sizeof...(T2)>0)read(RD2...);}void print(){putchar(PTEND);}void PRINT(int x){if(x<0)putchar('-'),x=-x;if(x>9) PRINT(x/10);putchar(x%10+'0');}template <typename T, typename... T2>void print(T PT,T2...PT2){if constexpr(is_same_v<T,int>)PRINT(PT);else cout<<PT;putchar(PTMID);print(PT2...);}

signed main()
{
    
    return 0;
}
```