# 链式前向星

```cpp
struct edge
{
	int from,to,w;
	int nxt;
}e[M];
int tot,lin[N];
void add(int u,int v,int w)
{
	e[++tot]=(edge){u,v,w};
	e[tot].nxt=lin[u];
	lin[u]=tot;
}
for(int i=lin[x];i;i=e[i].nxt)
{
	visit(e[i].to);
}
```