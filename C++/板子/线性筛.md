# 线性筛


[P3383 【模板】线性筛素数](https://www.luogu.com.cn/problem/P3383)

- 可用bitset降低空间

```cpp
for(int i=2;i<=n;i++)
{
	if(!s[i]) p[++cnt]=i;
	for(int j=1;j<=cnt;j++)
	{
		if(i*p[j]>n) break;
		s[i*p[j]]=1;
		if(i%p[j]==0) break;
	}
}
```