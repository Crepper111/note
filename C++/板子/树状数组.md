# 树状数组


[P3374 【模板】树状数组 1](https://www.luogu.com.cn/problem/P3374)

```cpp
void add(int x,int y)
{
	while(x<=n)
	{
		t[x]+=y;
		x+=x&(-x);
	}
}
int find(int x)
{
	int tmp=0;
	while(x)
	{
		tmp+=t[x];
		x-=x&(-x);
	}
	return tmp;
}
```